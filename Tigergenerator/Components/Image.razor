<img @ref="imgElement" src="@Src" hidden />

@code {
    private ElementReference imgElement;

    [Parameter]
    public string Id { get; set; }    

    [Parameter]
    public string Dir { get; set; }

    [Parameter]
    public string Src { get; set; }

    [Parameter]
    public IDictionary<string, ElementReference> Items { get; set; }

    protected override void OnAfterRender(bool firstRender)
    {
        if(Items == null)
            return;
            
        Items[$"{Dir}.{Id}"] = imgElement;
    }
}  